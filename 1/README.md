# All-FROND_LANG
Тут ви знайдете все про HTML, CSS, SCSS тощо.

---

## 📚 Перший урок: Основи HTML

Поїхали! Т_Т

---

### 1. Створення файлу `index.html`
Створіть файл `index.html`, щоб почати роботу.

---

### 2. Як створити головний скелет HTML?

Все дуже просто! Якщо виникнуть питання, ось стандартний шаблон:

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    
</body>
</html>
```

Щоб швидко створити такий скелет, просто напишіть ! і натисніть Tab або Enter (цей шорткат працює в більшості редакторів коду, наприклад, Visual Studio Code).

### 3. Чи можна вкладати якийсь елемент в якийсь, і як це перевірти?

Наприклад:

```html
<p><h1></h1></p>
```
Виникло питання, чи це допустимо? Для цього звернемося до документації HTML. Ви можете знайти всю необхідну інформацію за цим посиланням:
👉 [HTML Spec](https://html.spec.whatwg.org/multipage/)

## Як знайти відповідь:

1. Перейдіть до розділу **4 The elements of HTML**.
2. Відкрийте підрозділ **4.4 Grouping content**.
3. Перегляньте опис елементів, які вас цікавлять. У нашому випадку це:
   - **4.4.1 The p element**
   - **4.3.6 The h1, h2, h3, h4, h5, and h6 elements**

## Що ми бачимо в документації?

- Для **4.4.1 The p element** ідемо в контентну модель:
  - Контентний атрибут (контентна модель):
    - Global attributes (Глобальні атрибути)

- Для **4.3.6 The h1, h2, h3, h4, h5, and h6 elements** ідемо в категорії:
  - Категорії:
    - Flow content
    - Heading content
    - Palpable content

Значить Global attributes не можуть бути в h1 оскільки в категорії h1 НЕМАЄ Global attributes, отже p не може бути в h1.:

## Висновок:

Елементи `<h1>` та `<p>` **не можуть бути використані разом**.  
Причина в тому, що `<h1>` відноситься до **Heading content** (заголовки), тоді як `<p>` є **параграфним елементом**. 

Якщо ви напишете код так:

```html
<p><h1>Заголовок</h1></p>
```

Браузер автоматично інтерпретує його так:

```html
<p></p>
<h1>Заголовок</h1>
<p></p>
```
Тому правильно це робиться так такими варіантами:

```html
<p>Here....</p>
<h1>Заголовок</h1>
```
```html
<div>
    <p>Here....</p>
    <h1>Заголовок</h1>
</div>
```

## Давайте візьмемо інший приклад:

1. Перейдіть до розділу **4 The elements of HTML**.
2. Відкрийте підрозділ **4.4 Grouping content**.
3. Перегляньте опис елементів, які вас цікавлять. У нашому випадку це:
   - **4.4.6 The ul element**
   - **4.4.8 The li element**

## Що ми бачимо в документації?

- Для **4.4.6 The ul element** ідемо в контентну модель:
  - Контентний атрибут:
    - Content model: Zero or more li and script-supporting elements.

- Для **4.4.8 The li element** ідемо в категорії:
  - Категорії:
    - None

Отже, ul може бути в li (навіть сам ul нам про це каже), але li не може бути в ul:

```html
<ul>
    <li></li>
    <li></li>
</ul>
```

Тобто ми порівнюємо наш перший елемент з другим:
Ставимо питання: - Чи можна section стативи h1?
ідемо до документації, section, дивимось в контентну модель: 
- Flow content.
Ідемо в документації h1, дивимось в категорії: 
- Flow content.
- Heading content.
- Palpable content.

Оп в категорію h1 входить Flow content, значить вони можуть бути разом.

### 4. Що таке абсолютний шлях і відносний шлях?

Абсолютний шлях це повний шлях до файла з інтернету, наприклад:

```html
src="https://images.pexels.com/photos/21336456/pexels-photo-21336456/free-photo-of-paysage-desert-voyager-etats-unis.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=1"
```

Відносний шлях це коли шлях будується в нашому локальному проекті звідки ми дістаємо наше (локальне) фото:

```html
src="./images/pexels-pixabay-21336456.jpg" # Увага, тут вам потрібно створити папку images і в ню скачати фото і назвати саме так як на прикладі pexels-pixabay-21336456.jpg, якщо ви міняєте назву фото, змініть її і в посиланні, а то браузер вам її не покаже, тобто: ./images/<назва фотографії>.<формат фотографії>
```

Тобто там посилання на картинку з інтернету, а там посилання на картинку в нашому проекті (локальне посилання), папці (локальна папка) і тд. Спробуйте:

```html
<a href="https://images.pexels.com/photos/21336456/pexels-photo-21336456/free-photo-of-paysage-desert-voyager-etats-unis.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=1"></a>
```
І 

```html
<a href="./images/pexels-pixabay-21336456.jpg"></a>
```

### 5. Растрова графіка і векторна графіка?

Растрова графіка це карта кольорів на якомусь певному полотні, для прикладу у нас є полотно 100 на 100, і певні його клітини є замальовані якиймось кольором(рами).
Векторна графіка це набір формул (тобто точок) по якому браузер має намалювати (те саме коли він малює текст)

Підсумок:
Векторна графіка при маштубванні веб-сторінки не втрачатиме свою якість, поки ж растрова графіка при маштабуванні веб-сторінки втрачає свою якість, тобто розпливається, розмивається і тд.

Формати векторних графіків:
- SVG (Scalable Vector Graphics)
- PDF (Portable Document Format)
- EPS (Encapsulated PostScript)
- AI (Adobe Illustrator)
- DXF (Design Exchange Format)
- WMF (Windows Metafile)

Формати растрових графіків:
- JPEG (Joint Photographic Experts Group)
- PNG (Portable Network Graphics)
- GIF (Graphic Interchange Format)
- BMP (Bit Map)
- WEBP
- RAW

### 6. Контентне, декоративне і фонове зображення - різниця?

**Контентне зображення** - це як лого на веб-сторінці, оскільки це символ нашої компанії, тому це **ЗАВЖДИ** <img />
**Декоративні зображення** - це зображення, яке не несе інформаційного навантаження і служить лише для покращення зовнішнього вигляду сторінки, наприклад, ілюстрації або декоративні елементи.
**Фонові зображення** - це зображення, яке використовується як фон на сторінці або елементах веб-сторінки, наприклад, фон на веб-сайті чи фон для певних блоків контенту. Ці зображення зазвичай додаються через CSS, а не за допомогою тега <img />

### 6.1. Навігація - це ОБОВ'ЯЗКОВО ЗАВЖДИ СПИСОК! ОБОВ'ЯЗКОВО:

```html
ul>li*3>a[href="#"]
```

```html
<ul>
    <li><a href="#"></a></li>
    <li><a href="#"></a></li>
    <li><a href="#"></a></li>
</ul>
```

### 7. В header, коли нам потрібно ставити посилання а коли кнопку?

Якщо ви знаєте що при нажиманні на це має відкритись якесь модальне вікно, чи виконатись якась дія (підтвердити, скасувати і тд), тоді це кнопка (<button></button>)
Якщо ви знаєте що при нажиманні на це вас має перенести в іншу сторінку, тому подібне, тоді це посилання (<a></a>)

### 8. Чому ul>li, а не nav>ul>li?

<nav> - Визначає розділ основної навігації з посиланнями на секції поточної або інших сторінок. Використовується тільки для основної навігації, а не для будь-якої групи посилань в документі (Тобто в хедері), але не в footer.

**УВАГА** ПОСИЛАННЯ НА СОЦ МЕРЕЖІ - це не nav, також якби в header були б кнопки в nav вони б теж не входили

Ви можете не використовувати nav, але просто бажано щоб веб-сайт розумів що там є ГОЛОВНІ посилання, тому як бажаєте, і так і так все буде працювати